{
  "name": "bentodrop",
  "version": "0.1.0",
  "private": true,
  "description": "BentoDrop 便當 — cross-device end-to-end encrypted inbox on Cloudflare Workers",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler d1 migrations apply bentodrop --remote && wrangler deploy",
    "setup": "node scripts/setup.mjs",
    "gen-vapid": "node scripts/gen-vapid.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "test:all": "npm run typecheck && npm run test && npm run test:e2e"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.22.0",
    "@cloudflare/workers-types": "^5.20260827.1",
    "@playwright/test": "^1.58.0",
    "@scure/bip39": "^2.3.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.0"
  }
}
